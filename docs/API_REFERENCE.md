# Довідник API (API Reference)

Технічний опис основних функцій та змінних **AI SOUL**.

## 🌍 Глобальні Об'єкти

### `STATE`
Зберігає поточний стан додатку.
*   `hands` (Object): Дані про руки, ключі - ID рук.
    *   `x, y, z` (Number): Координати центру долоні.
    *   `pinch` (Boolean): Чи зведені пальці ("щипок").
    *   `pos` (THREE.Vector3): Позиція у 3D світі.
*   `ar` (Boolean): Чи увімкнено режим доповненої реальності (прозорість).
*   `stressEMA` (Number): Згладжений показник "стресу" (0.0 - 1.0).
*   `calibrated` (Boolean): Чи відкалібрована рука користувача.
*   `gesture` (String): Останній розпізнаний жест ('circle', 'swipe-up', etc.).

### `CFG`
Конфігурація (налаштування).
*   `radius` (Number): Радіус основної сфери (8).
*   `pushRange` (Number): Радіус впливу відштовхування (16).
*   `gripStrength` (Number): Сила захоплення (0.35).
*   `spring` (Number): Жорсткість пружин сфери (0.05 - 0.15).

## 🛠 Основні Функції

### `initSystem()`
`async function`
Ініціалізує всі підсистеми: AudioContext, WebGL, MediaPipe, VoiceCommand. Запускається натисканням кнопки "CONNECT".

### `processResults(results)`
Callback-функція для MediaPipe Hands.
*   `results` (Object): Сирі дані від MediaPipe.
*   Виконує: Нормалізацію координат, розпізнавання жестів, оновлення фізичного стану рук.

### `updatePhysicsAndVisuals()`
Основний цикл фізики (викликається в `animate`).
*   Розраховує пружинну модель сфери.
*   Обробляє колізії (Push) та захоплення (Grip).
*   Оновлює позиції AR-кубів та пульсацію нейронної мережі.
*   Оновлює кольори вершин залежно від стресу.

### `drawTechHUD(lm, id, isPinch, hand)`
Малює 2D інтерфейс поверх 3D сцени.
*   `lm`: Масив точок руки (landmarks).
*   Малює скелет, кути суглобів, вектори та текстову інформацію.

### `Calibration.addSample(handSpan)`
Додає вимір розмаху долоні для калібрування масштабу.
*   `handSpan`: Відстань між зап'ястям та пальцями.
*   Після 30 зразків розраховує `CFG.handScale`.

### `VoiceCommand`
Об'єкт управління голосом.
*   `init()`: Перевіряє підтримку API.
*   `start()`: Запускає постійне прослуховування.
*   `commands`: Словник команд ('stop', 'reset', 'bigger'...).
